export default class Store {

  constructor( opts ) {
  
  }

}

export class ChannelStore extends Store {
  constructor( opts ) {
    super(opts);
  }
}

export class UserStore extends Store {
  constructor( opts ) {
    super(opts);
  }
}
